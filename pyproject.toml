[project]
name = "peopleos-brief"
version = "0.1.0"
description = "PeopleOS Brief daily executive intelligence dashboard and newsletter"
requires-python = ">=3.12"
dependencies = [
  "groq>=0.9.0",
  "tavily-python>=0.3.0",
  "resend>=2.0.0",
  "supabase>=2.0.0",
  "python-dotenv>=1.0.0",
  "pytest>=8.0.0",
]

[tool.vercel]
entrypoint = "api.admin:handler"
